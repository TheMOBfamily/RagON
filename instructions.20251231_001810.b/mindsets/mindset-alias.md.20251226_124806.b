# Alias Quick Reference

> One-line lookup. `{alias}` → target.

---

## Mindsets

`{aphoristic}` `{khau huyet}` → mindset-aphoristic-writing.md
`{agile}` `{kaizen}` `{baby steps}` → mindset-agile-kaizen-think-big-do-baby-steps.md
`{backup}` `{safety first}` → mindset-always-backup-first.md
`{evidence first}` `{anti-bias}` → mindset-anti-confirmation-bias-evidence-first.md
`{auto debug}` `{no quit}` → mindset-auto-run-auto-debug-auto-fix.md
`{5w1h}` `{6 hats}` `{brainstorm}` → mindset-brainstorm-5w1h-6-thinking-hats.md
`{checklist}` `{9 steps}` → mindset-checklist-manifesto.md
`{double check}` `{verify twice}` → mindset-double-check-cross-check.md
`{memetic}` `{lesson learned}` → mindset-memetic-lesson-learned.md
`{red team}` `{adversarial}` → mindset-proof-by-contradiction-...-red-team-....md
`{stateless}` → mindset-stateless.md
`{pareto}` `{8020}` `{80/20}` → mindset-pareto-80-20.md
`{math safe}` `{zero trust}` `{SCME}` → mindset-math-safe-zero-trust-arithmetic.md
`{alias}` `{aliases}` → mindset-alias.md

## Instructions

`{fongtools}` → fongtools.json
`{-> .memory}` `{fongmemory}` → fongmemory.md
`{docs}` → docs/
`{todo later}` → docs/todo-later.md
`{fnote}` `{obsidian}` → MCP fnote tools
`{notion}` `{fongnotion}` → fongnotion.mdfon
`{veta}` → instructions-VETA-Viet-Eng-Term-Analyzer.json
`{color}` → LaTeX/04-color-palette.md
`{latex guide}` `{tikz guide}` → LaTeX/
`{latex debug}` `{tikz debug}` → LaTeX/07-build-watch-debug.md
`{textor}` `{md to pdf}` `{mermaid}` `{plantuml}` → textor/
`{copilot}` `{gpt}` → instructions-github-copilot-cli/
`{rcifeni}` → instructions-rcifeni-o-prompt-engineering.md
`{pullpush}` `{sync projects}` → instructions-push-pull-boiler-plate.md
`{source init prompt}` → /home/fong/Projects/boiler-plate-cursor-project-with-init-prompt/.fong/init-prompt.md
`{clear backups}` `{cleanup .b files}` → ../tools/cleanup-backup-files.sh
`{bpmn}` → BPMN/bpmn-vi.md
`{asana}` `{fongasana}` → fongasana/
`{ftask}` → ftask/
`{arxiv}` → instructions-proper-arxiv-searcher.md
`{autochrome}` → instructions-autochrome.md
`{doc principles}` → instructions-documentation-principles-for-ai/
`{autoclaude}` → .fong/claude-code-automation/
`{claude memory}` `{update claude memory}` → Claude-update-memory.md
`{long context}` `{GK}` `{graph knowledge}` → long-context-orchestration-toolkit/instructions-long-context-orchestration-toolkit.md
`{tuc mang minh}` `{git history}` → instructions-tuc-mang-minh.md
`{smartsearch}` `{smart search}` → smartsearch.md

## Actions

`{open}` → `code -r /absolute/path/to/file` (VSCode, file only, NO FOLDER - folder = new window)
`{feynman}` → Apply Feynman style, don't advertise name
`{dropbox}` → /home/fong/Dropbox/ai-shared (ưu tiên) | /home/fong/Dropbox (fallback)
`{count}` `{math count}` → MCP safe-calculation count operation

## DSS (Decision Support System)

`{DKM}` `{DSS}` → `{RAG}` + `{NewRAG}` + `{perplexity}` + `{copilot}` workflow
`{RAG}` `{NewRAG}` → MCP queryNewRAG (books, PDFs)
`{perplexity}` → MCP queryPerplexity (latest practices, real-time web)
`{copilot}`  → `.fong/instructions/instructions-github-copilot-cli/copilot-rcifeni-o.sh`


---

**Alias:** `{alias}`, `{aliases}`
